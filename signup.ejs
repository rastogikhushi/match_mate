<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
    font-family: 'Arial', sans-serif;
    margin: 0;
    padding: 0;
    background: linear-gradient(to right top, #ff85a1, #ffc3a0, #ffedd5);
    display: flex;
    align-items: center;
    justify-content: center;
    height: 100vh;
}
h1 {
    color: #fff;
    /* text-align: center; */
    margin-bottom: 20px;
    position: relative;
    bottom: 270px;
    left: 300px;
}
form {
    width: 100%;
    max-width: 400px;
    background: linear-gradient(to bottom, #ff9bb5, #ff5e62);
    padding: 30px;
    border-radius: 15px;
    box-shadow: 0 0 20px rgba(0, 0, 0, 0.3);
}

label {
    display: block;
    margin: 15px 0 8px;
    color: #fff;
    font-size: 16px;
}

input {
    width: 100%;
    padding: 12px;
    margin-bottom: 15px;
    box-sizing: border-box;
    border: 1px solid #fff;
    border-radius: 8px;
    background-color: rgba(255, 255, 255, 0.1);
    color: #fff;
}

button {
    background-color: #fff;
    color: #ff5e62;
    padding: 12px 20px;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    font-size: 16px;
    transition: background-color 0.3s ease, color 0.3s ease;
}

button:hover {
    background-color: #ff5e62;
    color: #fff;
}

/* Additional Styles for a Romantic Feel */

body::before {
    content: "";
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: url('romantic-background.jpg') center/cover no-repeat; /* Replace 'romantic-background.jpg' with your image path */
    z-index: -1;
    opacity: 0.2;
}

form {
    position: relative;
    z-index: 1;
}

h1 {
    color: #fff;
    text-align: center;
    margin-bottom: 30px;
}

    </style>
</head>
<body>
    <h1>Sign Up</h1>
    <form action="/register" method="post">
        <div>
            <label for="mail">Email</label>
            <input type="email" name="email" id="mail">
        </div>
        <div>
            <label for="name">Username</label>
            <input type="text" name="username" id="name">
        </div>
        <div>
            <label for="pass">Password</label>
            <input type="text" name="password" id="pass">
        </div>
        <button type="submit">Login</button>
    </form>
</body>
</html>